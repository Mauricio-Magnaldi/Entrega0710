<h2>Bienvenido</h2>
{{#if user}}
        <p>{{user.first_name}} {{user.last_name}}</p>
        <p>Tu email registrado es {{user.email}}</p>
    {{else}}
        <h2>Sin usuario para mostrar.</h2>
{{/if}}